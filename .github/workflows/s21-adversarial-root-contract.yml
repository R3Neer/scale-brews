name: s21-adversarial-root-contract

on:
  push:
    branches: [chatgpt-editing]
    paths:
      - '.github/workflows/s21-adversarial-root-contract.yml'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/api/CollisionEngines.java'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/api/spi/RootTransformProvider.java'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/internal/BuiltInRootTransformProviders.java'
      - 'src/gametest/java/io/github/r3neer/scalebrews/collision/internal/S21AdversarialRootTransformContractTests.java'
      - 'src/gametest/resources/fabric.mod.json'
  workflow_dispatch:

jobs:
  root-authority-spi:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: gradle/actions/wrapper-validation@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: microsoft
      - run: chmod +x ./gradlew
      - name: isolate S21 adversarial root-contract holdout
        shell: bash
        run: |
          # Independent implementer harnesses are currently red at compile time. They are unrelated
          # to this SPI holdout and are removed only inside this focused CI checkout.
          rm -f src/gametest/java/io/github/r3neer/scalebrews/test/S20CitadelPoseWorldClientProof.java
          rm -f src/gametest/java/io/github/r3neer/scalebrews/collision/internal/S21RootTransformProviderTests.java
          python3 - <<'PY'
          import json
          from pathlib import Path
          p=Path('src/gametest/resources/fabric.mod.json')
          data=json.loads(p.read_text())
          data['entrypoints']['fabric-gametest']=[
            'io.github.r3neer.scalebrews.collision.internal.S21AdversarialRootTransformContractTests'
          ]
          data['entrypoints']['fabric-client-gametest']=[]
          p.write_text(json.dumps(data,indent=2)+'\n')
          PY
      - name: run S21 adversarial root-contract holdout
        run: ./gradlew runGameTest --console=plain -PscalebrewsGameTestReport="$GITHUB_WORKSPACE/build/s21-adversarial-root-contract-junit.xml"
      - name: capture evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: S21-adversarial-root-contract
          path: |
            build/s21-adversarial-root-contract-junit.xml
            build/run/gameTest/logs/
            build/resources/gametest/fabric.mod.json
