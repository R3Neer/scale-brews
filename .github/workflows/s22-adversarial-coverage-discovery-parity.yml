name: s22-adversarial-coverage-discovery-parity

on:
  push:
    branches: [chatgpt-editing]
    paths:
      - '.github/workflows/s22-adversarial-coverage-discovery-parity.yml'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/catalog/CollisionCoverageDiscovery.java'
      - 'src/gametest/java/io/github/r3neer/scalebrews/collision/catalog/S22AdversarialCoverageDiscoveryParityTests.java'
  workflow_dispatch:

jobs:
  coverage-discovery-parity:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: gradle/actions/wrapper-validation@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: microsoft
      - run: chmod +x ./gradlew
      - name: isolate S22 vanilla discovery parity holdout
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          p=Path('src/gametest/resources/fabric.mod.json')
          data=json.loads(p.read_text())
          data['entrypoints']['fabric-gametest']=[
            'io.github.r3neer.scalebrews.collision.catalog.S22AdversarialCoverageDiscoveryParityTests'
          ]
          data['entrypoints']['fabric-client-gametest']=[]
          p.write_text(json.dumps(data,indent=2)+'\n')
          PY
      - name: run S22 vanilla discovery parity holdout
        run: ./gradlew runGameTest --console=plain -PscalebrewsGameTestReport="$GITHUB_WORKSPACE/build/s22-adversarial-coverage-discovery-parity-junit.xml"
      - name: capture evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: S22-adversarial-coverage-discovery-parity
          path: |
            build/s22-adversarial-coverage-discovery-parity-junit.xml
            build/run/gameTest/logs/
            build/resources/gametest/fabric.mod.json
