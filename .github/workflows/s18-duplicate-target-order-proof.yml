name: s18-duplicate-target-order-proof

on:
  push:
    branches: [chatgpt-editing]
    paths:
      - '.github/workflows/s18-duplicate-target-order-proof.yml'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/geometry/**'
      - 'src/main/java/io/github/r3neer/scalebrews/collision/pose/**'
      - 'src/client/java/io/github/r3neer/scalebrews/client/collision/preparation/**'
      - 'src/gametest/java/io/github/r3neer/scalebrews/test/S18DuplicateTargetAccumulationOrderClientTests.java'
  workflow_dispatch:

jobs:
  client-duplicate-target-order:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: gradle/actions/wrapper-validation@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: microsoft
      - run: chmod +x ./gradlew
      - name: isolate duplicate-target accumulation-order holdout
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path
          p=Path('src/gametest/resources/fabric.mod.json')
          data=json.loads(p.read_text())
          data['entrypoints']['fabric-client-gametest']=[
            'io.github.r3neer.scalebrews.test.S18DuplicateTargetAccumulationOrderClientTests'
          ]
          p.write_text(json.dumps(data,indent=2)+'\n')
          PY
      - name: run duplicate-target accumulation-order holdout
        run: xvfb-run -a ./gradlew runClientGameTest --console=plain
      - name: capture duplicate-target order evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: S18-duplicate-target-order-proof
          path: |
            build/run/clientGameTest/logs/
            build/resources/gametest/fabric.mod.json
